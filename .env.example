# Origin 2025b MCP Server Configuration

# Auto-connect to Origin on first tool call
ORIGIN_AUTO_CONNECT=true

# Default export settings
ORIGIN_DEFAULT_DPI=300
ORIGIN_DEFAULT_FORMAT=png
ORIGIN_EXPORT_DIR=./exports

# Debug mode (enables verbose logging via MCP context)
ORIGIN_DEBUG=false
